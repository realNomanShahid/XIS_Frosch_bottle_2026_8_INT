# A03 evidence

## Primary record
The dataset inventory is:

- `docs/artifacts/A03.md`


## Supporting material in the workspace
- Inventory narrative only in the A03 artifact (no raw image dump in repo — by design)
- Operational sample **outputs** (not the training set) under:
  - `bottle_500ml/`
  - `bottle_100ml/`
  - `bottle_300ml/`

